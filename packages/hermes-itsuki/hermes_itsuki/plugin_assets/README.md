# itsuki (deployed plugin)

This directory is written by `hermes-itsuki install`. Do not edit it by hand:
re-running the installer overwrites every file here from the installed package,
which is how the copy is kept from drifting.

Remove it with `hermes-itsuki uninstall`.
