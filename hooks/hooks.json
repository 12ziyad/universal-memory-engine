{
	"description": "Itsuki memory hooks: recall project memory at session start, save what the session learned at session end. Neither ever blocks the agent, and neither ever fails silently — if memory is off, they say so and why.",
	"hooks": {
		"SessionStart": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.mjs\"",
						"timeout": 15
					}
				]
			}
		],
		"SessionEnd": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/session-end.mjs\"",
						"timeout": 25
					}
				]
			}
		]
	}
}
